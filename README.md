Deploy Squid Proxy with basic authentication to AWS instance
--------------------------

###### Requirements:
- terraform
- ansible

###### Configure
- set proxy username and password in ansible playbook
- set proxy port

###### Deploy
```
terraform init
terraform apply
```
